<div class="dropdown-menu megamenu" role="menu">    <div class="leftmenu mb-3 ">
        <div class="row">
            <div class="col">
                <ul class="parent">
                    {%- for link in linklists.underwear.links -%}
                        <li {%-if link.active-%} class="active {%-if link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                            <a class="parent-link" href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
                            {%-if link.links != blank-%}
                                <ul class="child mt-4">
                                    {%-for child_link in link.links-%}
                                        <li {%-if child_link.active-%} class="active {%-if child_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                            <a href="{{ child_link.url }}" title="{{ child_link.title }}">{{ child_link.title }}</a>
                                            {%-if child_link.links != blank-%}
                                                <ul class="grandchild">
                                                    {%-for grandchild_link in child_link.links-%}
                                                        <li {%-if grandchild_link.active-%} class="active {%-if grandchild_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                            <a href="{{ grandchild_link.url }}" title="{{ grandchild_link.title }}">{{ grandchild_link.title }}</a>
                                                        </li>
                                                        {%-endfor-%}</ul>
                                                    {%-endif-%}</li>
                                                {%-endfor-%}</ul>
                                            {%-endif-%}</li>
                                    {%- endfor -%}
                                </ul>
                            </div>
                            <div class="col">
                                <ul class="parent">
                                    {%-for link in linklists.swimwear.links-%}
                                        <li {%-if link.active-%} class="active {%-if link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                            <a class="parent-link" href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
                                            {%-if link.links != blank-%}
                                                <ul class="child mt-4">
                                                    {%-for child_link in link.links-%}
                                                        <li {%-if child_link.active-%} class="active {%-if child_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                            <a href="{{ child_link.url }}" title="{{ child_link.title }}">{{ child_link.title }}</a>
                                                            {%-if child_link.links != blank-%}
                                                                <ul class="grandchild">
                                                                    {%-for grandchild_link in child_link.links-%}
                                                                        <li {%-if grandchild_link.active-%} class="active {%-if grandchild_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                            <a href="{{ grandchild_link.url }}" title="{{ grandchild_link.title }}">{{ grandchild_link.title }}</a>
                                                                        </li>
                                                                        {%-endfor-%}</ul>
                                                                    {%-endif-%}</li>
                                                                {%-endfor-%}</ul>
                                                            {%-endif-%}</li>
                                                        {%-endfor-%}</ul>
                                                </div>
                                                <div class="col">
                                                    <ul class="parent">
                                                        {%-for link in linklists.clothing.links-%}
                                                            <li {%-if link.active-%} class="active {%-if link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                <a class="parent-link" href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
                                                                {%-if link.links != blank-%}
                                                                    <ul class="child mt-4">
                                                                        {%-for child_link in link.links-%}
                                                                            <li {%-if child_link.active-%} class="active {%-if child_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                                <a href="{{ child_link.url }}" title="{{ child_link.title }}">{{ child_link.title }}</a>
                                                                                {%-if child_link.links != blank-%}
                                                                                    <ul class="grandchild">
                                                                                        {%-for grandchild_link in child_link.links-%}
                                                                                            <li {%-if grandchild_link.active-%} class="active {%-if grandchild_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                                                <a href="{{ grandchild_link.url }}" title="{{ grandchild_link.title }}">{{ grandchild_link.title }}</a>
                                                                                            </li>
                                                                                            {%-endfor-%}</ul>
                                                                                        {%-endif-%}</li>
                                                                                    {%-endfor-%}</ul>
                                                                                {%-endif-%}</li>
                                                                            {%-endfor-%}</ul>
                                                                    </div>
                                                                    <div class="col">
                                                                        <ul class="parent">
                                                                            {%-for link in linklists.accessories.links-%}
                                                                                <li {%-if link.active-%} class="active {%-if link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                                    <a class="parent-link" href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
                                                                                    {%-if link.links != blank-%}
                                                                                        <ul class="child mt-4">
                                                                                            {%-for child_link in link.links-%}
                                                                                                <li {%-if child_link.active-%} class="active {%-if child_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                                                    <a href="{{ child_link.url }}" title="{{ child_link.title }}">{{ child_link.title }}</a>
                                                                                                    {%-if child_link.links != blank-%}
                                                                                                        <ul class="grandchild">
                                                                                                            {%-for grandchild_link in child_link.links-%}
                                                                                                                <li {%-if grandchild_link.active-%} class="active {%-if grandchild_link.child_active-%}child-active{%-endif-%}" {%-endif-%}>
                                                                                                                    <a href="{{ grandchild_link.url }}" title="{{ grandchild_link.title }}">{{ grandchild_link.title }}</a>
                                                                                                                </li>
                                                                                                                {%-endfor-%}</ul>
                                                                                                            {%-endif-%}</li>
                                                                                                        {%-endfor-%}</ul>
                                                                                                    {%-endif-%}</li>
                                                                                                {%-endfor-%}</ul>
                                                                                        </div>
                                                                                        <div class="col">
                                                                                            <ul>
                                                                                                {%-for link in linklists.misc.links-%}
                                                                                                      {% if collection.products > 0 %}
                                                                                                    <li class="mb-3">
                                                                                                        <a class="parent-link" href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
                                                                                                    </li>
                                                                                                    {% endif %}
                                                                                                    {%-endfor-%}</ul>
                                                                                            </div>
                                                                                        </div>
                                                                                    </div>
                                                                                </div>